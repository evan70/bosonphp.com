doctrine_migrations:
  migrations_paths:
    # namespace is arbitrary but should be different from App\Migrations
    # as migrations classes should NOT be autoloaded
    'App\Shared\Infrastructure\Persistence\Doctrine\Migration': '%kernel.project_dir%/app/Shared/Infrastructure/Persistence/Doctrine/Migration'
  enable_profiler: false
